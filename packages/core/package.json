{
  "name": "@grandpa/core",
  "version": "0.1.0",
  "description": "Core functionality for the CLI application",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "files": ["src"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "inquirer": "^12.0.0",
    "chalk": "^5.4.1",
    "ora": "^8.1.1",
    "zod": "^3.24.1",
    "dotenv": "^16.4.7",
    "conf": "^13.0.1"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.7",
    "tsup": "^8.3.5"
  }
}